# Spec 006 / Phase 1 T008 — module skeleton.
# Phase 2 T013 will provision three SQL containers on the existing canonical
# database: registry-entities (PK /environment), registry-audit (PK /entityId),
# registry-entities-leases (PK /id). See specs/006-service-bus-registry-core/
# data-model.md §4.1 for container settings.
